# Command Library

This directory contains reusable commands for developing and maintaining
the Personal AI System.

Each command entry should explain:

- What the command does
- Where it should be run
- Whether it changes files or data
- Whether it is safe to repeat
- What result to expect
- Any important warnings

## Repository navigation

Run from the repository root:

```bash
cd /home/riley/workspace/personal-ai-system
