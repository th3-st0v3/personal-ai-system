# Next Runtime Work

Implement conditional continuation prompting in code: full project context once per valid active conversation; compact continuation prompts while the active task remains unfinished; full context only after context loss, new conversation, material project change, missing required state, or recovery.

Then implement a live task inbox and the large engineering task order so completed work automatically yields the next substantial objective.